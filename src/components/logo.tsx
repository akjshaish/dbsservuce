
import { Mountain } from 'lucide-react';

export function Logo() {
  return (
    <div className="flex items-center gap-2 text-primary font-semibold">
      <span className="text-2xl font-headline text-primary font-bold">AquaHost</span>
    </div>
  );
}
